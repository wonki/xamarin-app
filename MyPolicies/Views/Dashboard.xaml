<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyPolicies.Views.Dashboard">

    <Grid Margin="10,5,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"  />
            <RowDefinition Height="60" />
            <RowDefinition Height="60" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label Text="{Binding Title}" FontSize="Title" BackgroundColor="CornflowerBlue" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"/>
        <Button Text="My Policies" Clicked="OnMyPoliciesClicked"
                HorizontalOptions="Center" HeightRequest="100" WidthRequest="200" Grid.Row="1" Grid.Column="0"/>

        <Button Text="Add Car" Clicked="OnAddCarClicked"
                HorizontalOptions="Center" HeightRequest="100" WidthRequest="200" Grid.Row="1" Grid.Column="1"/>


        <Button Text="Add Motorcycle" Clicked="OnAddMotorcycleClicked"
                HorizontalOptions="Center" HeightRequest="100"  WidthRequest="200" Grid.Row="2" Grid.Column="0"/>
    </Grid>

</ContentPage>